services:
  - type: web
    name: booking-system-backend
    runtime: node
    rootDir: backend
    buildCommand: pnpm install --frozen-lockfile && pnpm run prisma:generate && pnpm run build
    startCommand: pnpm exec prisma migrate deploy --schema=prisma/schema.prisma && pnpm run start
    autoDeploy: true
