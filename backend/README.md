# Backend

## Local database

Start PostgreSQL locally with Docker:

```bash
pnpm run db:start
```

Run Prisma migrations:

```bash
pnpm run prisma:migrate
```

Start the API:

```bash
pnpm run dev
```
